# Healing Without — Take-Home Workspace

Drop this folder anywhere. It contains everything captured about the project so
you (or any AI assistant) can pick up where we left off.

## Files
- `GAME.md` — full project knowledge: structure, scripts, scenes, dialogic,
  audio, fonts, interaction flow, what was already cleaned up, and known gaps.
- `HERMES_SETTINGS_EXPORT.md` — how to reproduce the assistant's behavior at
  home (model/endpoint, config values, and a copy-paste system prompt).
- `PROJECT_INVENTORY.txt` — flat file/line inventory of the live project
  (handy for quick grep-free reference).

## How to use
1. Read `GAME.md` first for orientation.
2. To replicate the assistant setup elsewhere, follow `HERMES_SETTINGS_EXPORT.md`.
3. When resuming work, point an assistant at `GAME.md` and the project root
   `C:\Git\godot\Healing Without`.

## Standing constraints (carried from the working session)
- Do NOT commit or push without explicit instruction.
- Leave the `notes/` folder alone (story content).
- Do NOT "fix" the intentionally-skewed node transforms in `our_home.tscn`.
- The `sarah_door` object and `first-big-choice.dtl` are intentional and for
  imminent use — keep them.

Generated 2026-08-19.
